const path = require('path');

const
    APP_DIRECTORY_PATH = path.resolve(process.cwd(), './src'),
    CLIENT_ENTRY_PATH = path.resolve(process.cwd(), './root/client.js'),
    SERVER_ENTRY_PATH = path.resolve(process.cwd(), './root/server.js');

